# ERAN DESIGN 环境变量模板
# 复制为 .env 并填写真实值（.env 已被 gitignore，不会提交）

# 管理员后台密码（务必修改为强密码）
ADMIN_PASSWORD=your_strong_password_here

# Session 签名密钥（生产环境务必改为随机强字符串，可用 openssl rand -hex 32 生成）
SESSION_SECRET=your_random_session_secret_here
